{
  "name": "@bab/mcp-adapter",
  "version": "1.0.0",
  "description": "MCP Adapter for Browser AI Bridge - MCP as adapter, not foundation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "test": "vitest run"
  },
  "dependencies": {
    "@bab/protocol": "workspace:*",
    "@bab/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^3.0.0",
    "@types/node": "^22.0.0"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
